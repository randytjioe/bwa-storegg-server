{
  "version": 2,
  "builds": [
    {
      "src": "./bin/www",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "source": "/(.*)",
      "destination": "/bin/www",
      "methods": ["GET", "PUT", "POST", "DELETE", "OPTIONS", "PATCH"]
    }
  ]
}
